@model IEnumerable<GardenHub.Models.GardenCareActivity>

@{
    ViewData["Title"] = "Garden Care Activities";
    var gardenId = ViewData["GardenId"] as int?;
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-4"><i class="bi bi-clipboard-check"></i> Garden Care Activities</h1>
        <div class="d-flex gap-2">
            @if (gardenId.HasValue)
            {
                <a asp-action="Create" asp-route-gardenId="@gardenId" class="btn btn-success btn-lg">
                    <i class="bi bi-plus-circle"></i> Record Care Activity
                </a>
                <a asp-controller="Gardens" asp-action="Dashboard" asp-route-id="@gardenId" class="btn btn-secondary btn-lg">
                    <i class="bi bi-arrow-left"></i> Back to Dashboard
                </a>
            }
            else
            {
                <a asp-action="Create" class="btn btn-success btn-lg">
                    <i class="bi bi-plus-circle"></i> Record Care Activity
                </a>
            }
        </div>
    </div>

    @if (Model.Any())
    {
        <!-- Statistics Cards -->
        <div class="row g-3 mb-4">
            <div class="col">
                <div class="card bg-success text-white shadow h-100">
                    <div class="card-body text-center py-3">
                        <i class="bi bi-clipboard-check fs-2"></i>
                        <h4 class="mt-2 mb-0">@Model.Count()</h4>
                        <small>Total Activities</small>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="card bg-primary text-white shadow h-100">
                    <div class="card-body text-center py-3">
                        <i class="bi bi-droplet-fill fs-2"></i>
                        <h4 class="mt-2 mb-0">@Model.Count(a => a.WateringPerformed)</h4>
                        <small>Watering Sessions</small>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="card bg-info text-white shadow h-100">
                    <div class="card-body text-center py-3">
                        <i class="bi bi-moisture fs-2"></i>
                        <h4 class="mt-2 mb-0">@Model.Count(a => a.NutrientsAdded)</h4>
                        <small>Nutrient Applications</small>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="card bg-warning text-dark shadow h-100">
                    <div class="card-body text-center py-3">
                        <i class="bi bi-flower3 fs-2"></i>
                        <h4 class="mt-2 mb-0">@Model.Count(a => a.NewPlantingsAdded)</h4>
                        <small>New Plantings</small>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="card bg-danger text-white shadow h-100">
                    <div class="card-body text-center py-3">
                        <i class="bi bi-bug fs-2"></i>
                        <h4 class="mt-2 mb-0">@Model.Count(a => a.PestControlPerformed)</h4>
                        <small>Pest Control</small>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="card bg-dark text-white shadow h-100">
                    <div class="card-body text-center py-3">
                        <i class="bi bi-scissors fs-2"></i>
                        <h4 class="mt-2 mb-0">@Model.Count(a => a.PruningPerformed)</h4>
                        <small>Pruning Sessions</small>
                    </div>
                </div>
            </div>
        </div>

        <!-- DataTable Card -->
        <div class="card shadow">
            <div class="card-header bg-success text-white">
                <h3 class="mb-0"><i class="bi bi-table"></i> Care Activity Log</h3>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="careActivitiesTable" class="table table-striped table-hover table-bordered" style="width:100%">
                        <thead class="table-dark">
                            <tr>
                                <th>Date</th>
                                <th>Garden</th>
                                <th>Activity Type</th>
                                <th>Plant Status</th>
                                <th>Equipment Status</th>
                                <th>Water Level</th>
                                <th>Activities Performed</th>
                                <th>Duration</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td data-order="@item.ActivityDate.ToString("yyyy-MM-dd HH:mm")">
                                        @item.ActivityDate.ToString("MMM dd, yyyy")
                                        <br><small class="text-muted">@item.ActivityDate.ToString("h:mm tt")</small>
                                    </td>
                                    <td>
                                        <strong>@item.Garden?.GardenName</strong>
                                        <br><small class="text-muted">@item.Garden?.Type</small>
                                    </td>
                                    <td>
                                        <span class="badge bg-primary">@item.ActivityType</span>
                                    </td>
                                    <td>
                                        @if (item.PlantInspectionStatus.HasValue)
                                        {
                                            var statusBadge = item.PlantInspectionStatus == GardenHub.Models.Enums.PlantInspectionStatus.AllHealthy ? "success" :
                                                              item.PlantInspectionStatus == GardenHub.Models.Enums.PlantInspectionStatus.MinorIssues ? "warning" : "danger";
                                            <span class="badge bg-@statusBadge">@item.PlantInspectionStatus</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td>
                                        @if (item.EquipmentInspectionStatus.HasValue)
                                        {
                                            var eqBadge = item.EquipmentInspectionStatus == GardenHub.Models.Enums.EquipmentInspectionStatus.AllOperational ? "success" :
                                                          item.EquipmentInspectionStatus == GardenHub.Models.Enums.EquipmentInspectionStatus.MinorIssues ? "warning" : "danger";
                                            <span class="badge bg-@eqBadge">@item.EquipmentInspectionStatus</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td>
                                        @if (item.WaterLevel.HasValue)
                                        {
                                            <text>@item.WaterLevel.Value gal</text>
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td>
                                        <div class="d-flex flex-wrap gap-1">
                                            @if (item.WateringPerformed)
                                            {
                                                <span class="badge bg-info" title="Watering"><i class="bi bi-droplet-fill"></i></span>
                                            }
                                            @if (item.NutrientsAdded)
                                            {
                                                <span class="badge bg-success" title="Nutrients"><i class="bi bi-moisture"></i></span>
                                            }
                                            @if (item.NewPlantingsAdded)
                                            {
                                                <span class="badge bg-primary" title="New Plantings"><i class="bi bi-flower3"></i></span>
                                            }
                                            @if (item.WeedingPerformed)
                                            {
                                                <span class="badge bg-warning" title="Weeding"><i class="bi bi-exclamation-triangle"></i></span>
                                            }
                                            @if (item.PruningPerformed)
                                            {
                                                <span class="badge bg-secondary" title="Pruning"><i class="bi bi-scissors"></i></span>
                                            }
                                            @if (item.PestControlPerformed)
                                            {
                                                <span class="badge bg-danger" title="Pest Control"><i class="bi bi-bug"></i></span>
                                            }
                                        </div>
                                    </td>
                                    <td>
                                        @if (item.ActivityDuration.HasValue)
                                        {
                                            <text>@item.ActivityDuration min</text>
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td class="text-nowrap">
                                        <div class="btn-group btn-group-sm" role="group">
                                            <a asp-action="Details" asp-route-id="@item.ActivityId" class="btn btn-primary" title="Details">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                            <a asp-action="Edit" asp-route-id="@item.ActivityId" class="btn btn-warning" title="Edit">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@item.ActivityId" class="btn btn-danger" title="Delete">
                                                <i class="bi bi-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="card shadow">
            <div class="card-body text-center py-5">
                <i class="bi bi-clipboard-x display-1 text-muted mb-3"></i>
                <h3>No Garden Care Activities Recorded Yet</h3>
                <p class="text-muted">Start tracking your garden care activities to monitor your gardening tasks.</p>
                @if (gardenId.HasValue)
                {
                    <a asp-action="Create" asp-route-gardenId="@gardenId" class="btn btn-success btn-lg mt-3">
                        <i class="bi bi-plus-circle"></i> Record First Activity
                    </a>
                }
                else
                {
                    <a asp-action="Create" class="btn btn-success btn-lg mt-3">
                        <i class="bi bi-plus-circle"></i> Record First Activity
                    </a>
                }
            </div>
        </div>
    }
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#careActivitiesTable').DataTable({
                responsive: true,
                pageLength: 10,
                lengthMenu: [[10, 25, 50, -1], [10, 25, 50, "All"]],
                order: [[0, 'desc']],
                dom: '<"row"<"col-sm-12 col-md-6"l><"col-sm-12 col-md-6"f>>' +
                     '<"row"<"col-sm-12"tr>>' +
                     '<"row"<"col-sm-12 col-md-5"i><"col-sm-12 col-md-7"p>>',
                language: {
                    search: "_INPUT_",
                    searchPlaceholder: "Search activities...",
                    lengthMenu: "Show _MENU_ activities",
                    info: "Showing _START_ to _END_ of _TOTAL_ activities",
                    infoEmpty: "No activities available",
                    infoFiltered: "(filtered from _MAX_ total activities)",
                    paginate: {
                        first: '<i class="bi bi-chevron-double-left"></i>',
                        previous: '<i class="bi bi-chevron-left"></i>',
                        next: '<i class="bi bi-chevron-right"></i>',
                        last: '<i class="bi bi-chevron-double-right"></i>'
                    }
                },
                columnDefs: [
                    { orderable: false, targets: [8] },
                    { className: 'text-center', targets: [3, 4, 5, 6, 7, 8] }
                ]
            });
        });
    </script>
}
